# прямые зависимости проекта (транзитивные pip поставит сам)
# NLP для македонского: tokenize, POS, lemma, dependency parsing
classla>=2.0
# dependency parsing и NER через модель mk_core_news_lg
spacy>=3.8
# графовый анализ: построение и обход семантических графов
networkx>=3.0
# интерактивная визуализация графов в HTML
pyvis>=0.3
# статические графики и диаграммы
matplotlib>=3.5
# работа с таблицами и датасетами
pandas>=2.0
# числовые массивы и линейная алгебра
numpy>=1.24
# научные вычисления: SVD, разреженные матрицы
scipy>=1.10
# нейросеть: PyTorch
torch>=2.0
# токенизация предложений
nltk>=3.8
# FastText-эмбеддинги для македонского (cc.mk.300.bin)
# на Linux/macOS ставится fasttext, на Windows — fasttext-wheel (pre-built)
fasttext-wheel>=0.9.2
# утилиты для работы с эмбеддингами (KeyedVectors как fallback)
gensim>=4.0
# сериализация Python-объектов в JSON
jsonpickle>=3.0
# метрики, cosine similarity
scikit-learn>=1.3
# чтение Excel-файлов (.xlsx)
openpyxl>=3.1
# трансляция mk->ru через Helsinki-NLP/opus-mt-mk-ru
transformers>=4.30
# токенизатор для модели перевода
sentencepiece>=0.1.99
